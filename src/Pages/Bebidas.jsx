import React from 'react';
import SearchBar from '../Components/SearchBar';

const Bebidas = ({ history }) => (
  <div>
    <SearchBar history={history} />
    <h1>Bebidas</h1>
  </div>
);

export default Bebidas;
